#include "main.h"

/**
 * _isupper - checks if a letter is upper case or not
 * @c: char to be checked
 * Return: 1 if upper 0 if not
 */

int _isupper(int c)
{
	if (c >= 48 && c <= 57)
	{
	return (1);
	}
	else
	{
	return (0);
	}
}

